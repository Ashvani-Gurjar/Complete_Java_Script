//  let a = document.getElementsByTagName("div")[0]
//  a.innerHTML = a.innerHTML + "<h1>Good Morning Sir</h1>"


//  let div = document.createElement("div")
//  div.innerHTML = "<h1>Good Boy</h1>"
//  a.append(div)  // div container ke last mai put karega
//  a.prepend(div)  // div container ke first mai put karega
//  a.before(div)  // insert before node
//  a.after(div)  // insert after node
// a.replaceWith(div)  // Div container ke sthan per ye naya div lag jayega

// Next lectures 

// let first  = document.getElementById("first")
// first.insertAdjacentHTML("beforebegin", '<div class="test">beforebegin</div>')
// first.insertAdjacentHTML("beforeend", '<div class="test">beforeend</div>')
// first.insertAdjacentHTML('afterbegin', '<div class="test">afterbegin</div>')
// first.insertAdjacentHTML('afterend', '<div class="test">afterend</div>')

